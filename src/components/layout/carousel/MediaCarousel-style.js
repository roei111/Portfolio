import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  carouselImage: {
    objectFit: "fill",
  },
  video: {
    width: "100% !important",
    height: "100% !important",
    margin: "0 !important",
  }
}));